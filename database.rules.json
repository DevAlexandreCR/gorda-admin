{
  "rules": {
    ".read": true,
    ".write": true,
    "services": {
      "$uid": {
        ".indexOn": ["created_at", "status"]
      }
    },
    "sessions": {
      ".indexOn": ["chat_id", "created_at"]
    }
  }
}