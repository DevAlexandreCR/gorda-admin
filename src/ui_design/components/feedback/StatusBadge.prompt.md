One-liner: Drop-in badge for a service's status — handles color + label from the status code.

```jsx
<StatusBadge status="in_progress" />
<StatusBadge status="terminated" />
```

Statuses: `pending`, `in_progress`, `terminated`/`completed`, `canceled`.
